FROM node:22-alpine
WORKDIR /usr/src/app

# install deps from lockfile (npm’s correct flag is "ci")
COPY package*.json ./
RUN npm ci

# copy the rest (will be overridden by the bind mount at runtime)
COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
